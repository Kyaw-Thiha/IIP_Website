---
export interface Props {
  src: string;
  width: string;
  height: string;
}

const { src, width, height } = Astro.props;
---

<iframe
  src={src}
  width={width}
  height={height}
  style="border:0;"
  allowfullscreen=""
  loading="lazy"
  referrerpolicy="no-referrer-when-downgrade"
>
</iframe>
